# Embedding Cache Architecture

## System Overview

```
┌──────────────────────────────────────────────────────────────────────┐
│                         Client Request                                │
│              "Find documents about AI safety"                         │
└─────────────────────────────┬────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    RAG Handler (chat_v2.go)                           │
│  - Receives user query                                               │
│  - Calls AgenticRAGService                                           │
└─────────────────────────────┬────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│              AgenticRAGService (agentic_rag.go)                       │
│  - Query expansion                                                   │
│  - Context selection                                                 │
│  - Calls HybridSearchService                                         │
└─────────────────────────────┬────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│             HybridSearchService (hybrid_search.go)                    │
│  - Combines BM25 + Vector search                                    │
│  - Calls EmbeddingService for query embedding                       │
└─────────────────────────────┬────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│              EmbeddingService (embedding.go)                          │
│  ┌────────────────────────────────────────────────────────┐         │
│  │ func GenerateEmbedding(text string) []float32          │         │
│  │   1. Check cache first                                 │         │
│  │   2. If miss: Call Ollama                              │         │
│  │   3. Store result in cache                             │         │
│  │   4. Return embedding                                  │         │
│  └────────────────────────────────────────────────────────┘         │
└─────────────────────────────┬────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                ▼                           ▼
┌──────────────────────────┐    ┌──────────────────────────┐
│  EmbeddingCacheService   │    │   Ollama API             │
│  (NEW - This PR)         │    │   (Expensive: 200ms)     │
│                          │    │                          │
│  - SHA256 content hash   │    │  POST /api/embeddings    │
│  - Redis storage         │    │  {                       │
│  - 2ms latency           │    │    model: "nomic-embed"  │
│  - Graceful degradation  │    │    prompt: "text..."     │
│  - Statistics tracking   │    │  }                       │
└──────────┬───────────────┘    └──────────────────────────┘
           │
           ▼
┌──────────────────────────┐
│       Redis              │
│                          │
│  Key Pattern:            │
│  embedding:text:a3c5e8d  │
│  embedding:image:b7f2a9  │
│                          │
│  Value (JSON):           │
│  {                       │
│    embedding: [0.1, ...],│
│    type: "text",         │
│    hash: "a3c5e8d...",   │
│    cached_at: "..."      │
│  }                       │
│                          │
│  TTL: 24h (text)         │
│       48h (image)        │
└──────────────────────────┘
```

## Request Flow: First Query (Cache Miss)

```
Time: 0ms
┌────────────┐
│   Client   │
└─────┬──────┘
      │ "Find AI safety docs"
      ▼
Time: 5ms
┌──────────────────┐
│   RAG Service    │
└─────┬────────────┘
      │ Needs embedding for query
      ▼
Time: 10ms
┌───────────────────────┐
│  EmbeddingService     │
│  GenerateEmbedding()  │
└─────┬─────────────────┘
      │ 1. Check cache
      ▼
Time: 12ms
┌──────────────────────────┐
│  EmbeddingCacheService   │
│  GetEmbedding()          │
└─────┬────────────────────┘
      │ Redis GET
      ▼
Time: 14ms
┌──────────────┐
│    Redis     │ → MISS (key not found)
└─────┬────────┘
      │ nil
      ▼
Time: 16ms
┌───────────────────────┐
│  EmbeddingService     │
│  (cache miss)         │
└─────┬─────────────────┘
      │ 2. Call Ollama
      ▼
Time: 216ms (200ms to compute)
┌──────────────┐
│    Ollama    │ → Returns [0.1, 0.2, ..., 0.768]
└─────┬────────┘
      │ embedding vector
      ▼
Time: 218ms
┌───────────────────────┐
│  EmbeddingService     │
│  (got result)         │
└─────┬─────────────────┘
      │ 3. Store in cache
      ▼
Time: 220ms
┌──────────────────────────┐
│  EmbeddingCacheService   │
│  SetEmbedding()          │
└─────┬────────────────────┘
      │ Redis SET with TTL
      ▼
Time: 222ms
┌──────────────┐
│    Redis     │ → Stored with 24h TTL
└─────┬────────┘
      │ OK
      ▼
Time: 224ms
┌───────────────────────┐
│  EmbeddingService     │
│  (cache stored)       │
└─────┬─────────────────┘
      │ Return embedding
      ▼
Time: 230ms
┌──────────────────┐
│   RAG Service    │ → Vector search with embedding
└─────┬────────────┘
      │ Results
      ▼
Time: 280ms
┌────────────┐
│   Client   │ → Response received
└────────────┘

Total: ~280ms (First query - cache miss)
```

## Request Flow: Second Query (Cache Hit)

```
Time: 0ms
┌────────────┐
│   Client   │
└─────┬──────┘
      │ "Find AI safety docs" (same query)
      ▼
Time: 5ms
┌──────────────────┐
│   RAG Service    │
└─────┬────────────┘
      │ Needs embedding for query
      ▼
Time: 10ms
┌───────────────────────┐
│  EmbeddingService     │
│  GenerateEmbedding()  │
└─────┬─────────────────┘
      │ 1. Check cache
      ▼
Time: 12ms
┌──────────────────────────┐
│  EmbeddingCacheService   │
│  GetEmbedding()          │
└─────┬────────────────────┘
      │ Redis GET
      ▼
Time: 14ms
┌──────────────┐
│    Redis     │ → HIT! Returns cached embedding
└─────┬────────┘
      │ [0.1, 0.2, ..., 0.768]
      ▼
Time: 16ms
┌───────────────────────┐
│  EmbeddingService     │
│  (cache hit!)         │
└─────┬─────────────────┘
      │ Return embedding (skip Ollama)
      ▼
Time: 18ms
┌──────────────────┐
│   RAG Service    │ → Vector search with embedding
└─────┬────────────┘
      │ Results
      ▼
Time: 68ms
┌────────────┐
│   Client   │ → Response received
└────────────┘

Total: ~68ms (Second query - cache hit)

Speedup: 280ms → 68ms (4x faster!)
Cost Savings: No Ollama call (free)
```

## Cache Key Generation

```
┌──────────────────────────────────────────────────────────────┐
│  Content: "What are the benefits of AI?"                     │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  SHA256 Hash                                                 │
│  Input: "What are the benefits of AI?"                       │
│  Output: a3c5e8d9f2b1c4a7e6d8f3b2c1a5e9d4b7f2a9c1e5d3b8f6  │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  Redis Key Construction                                      │
│  Format: embedding:<type>:<hash>                             │
│  Result: embedding:text:a3c5e8d9f2b1c4a7e6d8f3b2c1a5e9d4    │
└──────────────────────────────────────────────────────────────┘

Benefits:
✓ Same content → Same hash → Same key (deduplication)
✓ Different content → Different hash → Different key
✓ Content not exposed in Redis (security)
✓ No collisions (SHA256 is cryptographically strong)
✓ Fast lookup (Redis GET is O(1))
```

## Multi-Modal Support

```
┌─────────────────────────────────────────────────────────────────┐
│                     Text Embedding Request                      │
│  Content: "AI safety research"                                 │
│  Type: "text"                                                   │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│  Cache Key: embedding:text:5f3a8b2c9d4e1a7f6b8c3d2e9a1f5b4c7e  │
│  TTL: 24 hours                                                  │
│  Model: nomic-embed-text (768 dimensions)                      │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     Image Embedding Request                     │
│  Content: <base64-image-data>                                  │
│  Type: "image"                                                  │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│  Cache Key: embedding:image:9a2f7e1d4b8c6f3a9e2d1b5c8f4a7e3d1  │
│  TTL: 48 hours (longer for stable image content)               │
│  Model: CLIP (512 dimensions)                                  │
└─────────────────────────────────────────────────────────────────┘

Note: Different types use different cache keys and TTLs
```

## Graceful Degradation

```
Scenario 1: Redis Unavailable at Startup
┌──────────────────────────────────────────────────┐
│  Application Start                               │
│    → Initialize Redis client                     │
│    → Ping Redis: ERROR (connection refused)      │
│    → EmbeddingCacheService.enabled = false       │
│    → Log: "Cache disabled, Redis unavailable"   │
│    → Continue without cache                      │
└──────────────────────────────────────────────────┘

All cache operations become no-ops:
- GetEmbedding() → returns (nil, false, nil)
- SetEmbedding() → returns nil
- No errors thrown, graceful fallback

Scenario 2: Redis Fails During Operation
┌──────────────────────────────────────────────────┐
│  Cache Operation                                 │
│    → GetEmbedding("text")                        │
│    → Redis GET: ERROR (connection timeout)       │
│    → Log error                                   │
│    → Return (nil, false, nil) - treated as miss  │
│    → Application continues                       │
│    → Ollama called as fallback                   │
└──────────────────────────────────────────────────┘

No user-facing errors!

Scenario 3: Corrupted Cache Data
┌──────────────────────────────────────────────────┐
│  Cache Operation                                 │
│    → GetEmbedding("text")                        │
│    → Redis returns corrupted JSON                │
│    → Unmarshal fails                             │
│    → Delete corrupted entry                      │
│    → Return (nil, false, nil)                    │
│    → Recompute and cache fresh data              │
└──────────────────────────────────────────────────┘

Self-healing!
```

## Statistics Tracking

```
┌──────────────────────────────────────────────────────────────┐
│                    Cache Statistics                          │
│                                                              │
│  Atomic Counters (thread-safe):                             │
│    Hits:   atomic.Int64  → Incremented on cache hit         │
│    Misses: atomic.Int64  → Incremented on cache miss        │
│                                                              │
│  Calculated Metrics:                                         │
│    Size:     Count of keys in Redis (SCAN operation)        │
│    HitRate:  Hits / (Hits + Misses)                         │
│                                                              │
└──────────────────────────────────────────────────────────────┘

Example Timeline:
Time    Event               Hits  Misses  Size  HitRate
──────────────────────────────────────────────────────────
0s      Start               0     0       0     0.0%
1s      Query "AI"          0     1       1     0.0%    (miss)
2s      Query "AI"          1     1       1     50.0%   (hit!)
3s      Query "ML"          1     2       2     33.3%   (miss)
4s      Query "AI"          2     2       2     50.0%   (hit!)
5s      Query "ML"          3     2       2     60.0%   (hit!)
10s     Query "AI"          4     2       2     66.7%   (hit!)
15s     Query "DL"          4     3       3     57.1%   (miss)

After warmup (100+ queries):
Hit Rate typically: 70-90% (excellent caching!)
```

## Memory Usage

```
Single Embedding (768 dimensions, text):
┌────────────────────────────────────────┐
│  Component              Size            │
├────────────────────────────────────────┤
│  Float64 array          768 * 8 = 6KB  │
│  JSON overhead          ~200 bytes     │
│  Redis key              ~50 bytes      │
│  Redis metadata         ~100 bytes     │
├────────────────────────────────────────┤
│  Total per entry:       ~6.4 KB        │
└────────────────────────────────────────┘

Scaling:
  1,000 embeddings   = 6.4 MB
 10,000 embeddings   = 64 MB
100,000 embeddings   = 640 MB

Recommendation: Set Redis maxmemory and use LRU eviction
Example: maxmemory 2gb, maxmemory-policy allkeys-lru
```

## Comparison: RAGCacheService vs EmbeddingCacheService

```
┌─────────────────────────────────────────────────────────────────┐
│                      RAGCacheService                            │
│  (Existing - rag_cache.go)                                      │
├─────────────────────────────────────────────────────────────────┤
│  Purpose:       High-level query result caching                │
│  Caches:        - Hybrid search results                        │
│                 - Agentic RAG responses                         │
│                 - Query embeddings (float32)                    │
│  Key Format:    rag:hybrid:<query-hash>                        │
│  TTL:           15 minutes (queries)                            │
│                 24 hours (embeddings)                           │
│  Use Case:      Cache entire search result sets                │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  EmbeddingCacheService                          │
│  (NEW - embedding_cache_service.go)                             │
├─────────────────────────────────────────────────────────────────┤
│  Purpose:       Low-level embedding vector caching             │
│  Caches:        - Individual embedding vectors (float64)       │
│                 - Multi-modal (text, image)                     │
│  Key Format:    embedding:<type>:<content-hash>                │
│  TTL:           24 hours (text)                                 │
│                 48 hours (image)                                │
│  Use Case:      Cache individual embedding computations        │
│  Features:      - Better precision (float64)                   │
│                 - Graceful degradation                          │
│                 - Better statistics                             │
│                 - Health checks                                 │
└─────────────────────────────────────────────────────────────────┘

Recommendation: Use BOTH for maximum efficiency!
- EmbeddingCacheService: Caches expensive embedding computations
- RAGCacheService: Caches expensive search operations

Together they provide two-level caching:
Level 1 (Embedding): Skip Ollama calls
Level 2 (Search): Skip database queries
```

## Data Flow Example: Complete RAG Request

```
1. User Query: "What is reinforcement learning?"

2. Flow:
   ┌─────────────────────────────────────────────────────────────┐
   │ RAGCacheService: Check if query result is cached           │
   │   Key: rag:agentic:hash(query+params)                      │
   │   → MISS (first time asking this)                          │
   └─────────────────────────────────────────────────────────────┘
                               │
                               ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ Need to generate embedding for query                        │
   └─────────────────────────────────────────────────────────────┘
                               │
                               ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ EmbeddingCacheService: Check if embedding is cached        │
   │   Key: embedding:text:hash("What is reinforcement...")     │
   │   → MISS (first time embedding this text)                  │
   └─────────────────────────────────────────────────────────────┘
                               │
                               ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ Call Ollama to generate embedding (~200ms)                 │
   │   POST /api/embeddings                                      │
   │   → Returns [0.1, 0.2, ..., 0.768]                         │
   └─────────────────────────────────────────────────────────────┘
                               │
                               ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ EmbeddingCacheService: Store embedding                     │
   │   SET embedding:text:hash(...) = {...} TTL 24h             │
   └─────────────────────────────────────────────────────────────┘
                               │
                               ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ Perform vector search in PostgreSQL (~50ms)                │
   │   SELECT ... ORDER BY embedding <=> $1                     │
   └─────────────────────────────────────────────────────────────┘
                               │
                               ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ RAGCacheService: Store search results                      │
   │   SET rag:agentic:hash(...) = {results} TTL 15m            │
   └─────────────────────────────────────────────────────────────┘
                               │
                               ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ Return results to user                                      │
   │   Total time: ~280ms                                        │
   └─────────────────────────────────────────────────────────────┘

3. Same Query Again (5 minutes later):
   ┌─────────────────────────────────────────────────────────────┐
   │ RAGCacheService: Check cached results                      │
   │   → HIT! Return cached results                             │
   │   Total time: ~2ms (140x faster!)                          │
   └─────────────────────────────────────────────────────────────┘

4. Different Query, Similar Content (25 minutes later):
   RAG cache expired, but embedding cache still valid
   ┌─────────────────────────────────────────────────────────────┐
   │ RAGCacheService: MISS (expired after 15m)                  │
   │ EmbeddingCacheService: HIT! (valid for 24h)                │
   │   → Skip Ollama, use cached embedding                      │
   │   → Only need to do vector search                          │
   │   Total time: ~52ms (5x faster than cold start)            │
   └─────────────────────────────────────────────────────────────┘
```

This architecture provides:
- **4-140x speedup** depending on cache state
- **Significant cost savings** by reducing Ollama API calls
- **Graceful degradation** when Redis fails
- **Multi-level caching** for maximum efficiency
