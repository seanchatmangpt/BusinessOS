# LiveKit Test Configuration
# Minimal configuration for integration testing

port: 7880
keys:
  testkey: testsecret

log_level: info

room:
  empty_timeout: 60
  max_participants: 100

rtc:
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: false
